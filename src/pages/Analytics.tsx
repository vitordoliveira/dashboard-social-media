import React from 'react';
import { Box, Typography } from '@mui/material';

const Analytics: React.FC = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h5" gutterBottom>
        Analytics
      </Typography>
      {/* Implementação futura do analytics */}
    </Box>
  );
};

export default Analytics;