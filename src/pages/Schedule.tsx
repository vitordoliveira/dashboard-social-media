import React from 'react';
import { Box, Typography, Paper, Grid } from '@mui/material';

const Schedule: React.FC = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h5" gutterBottom>
        Agendar Posts
      </Typography>
      {/* Implementação futura do agendamento */}
    </Box>
  );
};

export default Schedule;